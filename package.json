{
	"name": "bwfnotify-pwa",
	"version": "1.0.0",
	"private": true,
	"description": "PWA notifications for live BWF matches involving Japanese players.",
	"homepage": "https://bwfnotify-pwa.amtt.workers.dev/",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/nematatu/BWFNotify-PWA.git"
	},
	"bugs": {
		"url": "https://github.com/nematatu/BWFNotify-PWA/issues"
	},
	"keywords": [
		"badminton",
		"bwf",
		"cloudflare-workers",
		"pwa",
		"web-push"
	],
	"license": "MIT",
	"packageManager": "bun@1.3.14",
	"engines": {
		"bun": ">=1.3.14"
	},
	"type": "module",
	"scripts": {
		"dev": "WRANGLER_LOG_PATH=.wrangler/logs wrangler dev --test-scheduled",
		"deploy": "wrangler deploy",
		"dry-run": "WRANGLER_LOG_PATH=.wrangler/logs wrangler deploy --dry-run",
		"types": "wrangler types",
		"types:check": "WRANGLER_LOG_PATH=.wrangler/logs wrangler types --check",
		"typecheck": "tsc --noEmit",
		"test": "bun run test:unit && bun run test:integration",
		"test:unit": "bun test ./test/",
		"test:integration": "WRANGLER_LOG_PATH=.wrangler/logs vitest run --config vitest.config.ts",
		"test:layout": "playwright test",
		"lint": "biome check .",
		"check": "bun run types:check && bun run typecheck && bun run lint && bun run test",
		"precommit": "bun run check && bun run test:layout && bun run dry-run",
		"format": "biome check --write .",
		"generate:vapid": "web-push generate-vapid-keys --json"
	},
	"dependencies": {
		"hono": "^4.12.30",
		"web-push": "^3.6.7"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.4",
		"@cloudflare/vitest-pool-workers": "^0.18.6",
		"@playwright/test": "^1.61.1",
		"@types/node": "^26.1.1",
		"@types/web-push": "^3.6.4",
		"typescript": "^7.0.2",
		"vitest": "^4.1.0",
		"wrangler": "^4.112.0"
	}
}
